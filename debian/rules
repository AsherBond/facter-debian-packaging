#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/rules/buildcore.mk

binary-install/facter::
	 ruby install.rb --destdir=$(CURDIR)/debian/$(cdbs_curpkg) --quick --no-rdoc
